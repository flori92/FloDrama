{
  "name": "flodrama-api",
  "version": "1.0.0",
  "description": "API Backend FloDrama sur Cloudflare Workers",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler publish",
    "format": "prettier --write '**/*.{js,json}'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "FloDrama Team",
  "license": "MIT",
  "dependencies": {
    "itty-router": "^4.0.13"
  },
  "devDependencies": {
    "prettier": "^2.8.8",
    "wrangler": "^4.14.1"
  }
}
