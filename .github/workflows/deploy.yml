name: CI/CD FloDrama

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

# Permissions nécessaires pour GitHub Pages
permissions:
  contents: write
  pages: write
  id-token: write

env:
  S3_BUCKET: flodrama-content-1745269660
  AWS_REGION: us-east-1

jobs:
  backend:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout du code
        uses: actions/checkout@v4
      
      - name: Configuration de Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'
          cache: 'pip'
      
      - name: Configuration de Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: Backend/package-lock.json
      
      - name: Installation des dépendances Python
        run: |
          python -m pip install --upgrade pip
          if [ -f Backend/requirements.txt ]; then
            pip install -r Backend/requirements.txt
          else
            pip install aiohttp beautifulsoup4 boto3 fastapi pymongo redis opensearch-py pydantic
          fi
      
      - name: Installation des dépendances Node.js
        working-directory: ./Backend
        run: |
          if [ -f package.json ]; then
            npm ci
          else
            echo "Aucun package.json trouvé dans le dossier Backend, création d'un fichier minimal"
            echo '{
              "name": "flodrama-backend",
              "version": "1.0.0",
              "private": true,
              "scripts": {
                "start": "node src/lambda/index.js"
              },
              "dependencies": {
                "aws-sdk": "^2.1500.0"
              }
            }' > package.json
            npm install
          fi
      
      - name: Configuration AWS
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}
          role-duration-seconds: 3600
          role-session-name: GitHubActionsSession
      
      # Utiliser le secret s'il existe, sinon garder la valeur par défaut
      - name: Configuration de l'ID CloudFront
        run: |
          echo "🔑 Configuration de l'ID CloudFront..."
          # Le secret sera utilisé s'il existe, sinon la valeur par défaut sera conservée
          if [ -n "${{ secrets.CLOUDFRONT_DISTRIBUTION_ID }}" ]; then
            echo "CLOUDFRONT_ID=${{ secrets.CLOUDFRONT_DISTRIBUTION_ID }}" >> $GITHUB_ENV
            echo "✅ ID CloudFront récupéré depuis les secrets"
          else
            echo "⚠️ Utilisation de l'ID CloudFront par défaut"
          fi
      
      - name: Exécution du scraping et génération des données
        run: |
          echo "🔍 Lancement du scraping et de la génération des données..."
          
          # Création du script temporaire pour simuler le scraping
          cat > temp_scraping.py << 'EOF'
          import json
          import random
          import os
          from datetime import datetime
          
          # Fonction pour générer des données de démonstration
          def generate_demo_content(count=20):
              content = []
              categories = ["drama", "anime", "movie"]
              countries = ["Korea", "Japan", "China", "Thailand"]
              
              for i in range(1, count + 1):
                  category = random.choice(categories)
                  country = random.choice(countries)
                  year = random.randint(2010, 2025)
                  rating = round(random.uniform(3.5, 9.8), 1)
                  
                  content.append({
                      "id": f"demo{i}",
                      "title": f"{country} {category.title()} {i}",
                      "original_title": f"Original Title {i}",
                      "description": f"This is a {category} from {country} released in {year}.",
                      "poster_url": f"https://via.placeholder.com/300x450?text={country}+{category}+{i}",
                      "backdrop_url": f"https://via.placeholder.com/1280x720?text={country}+{category}+{i}",
                      "year": year,
                      "country": country,
                      "category": category,
                      "genres": random.sample(["action", "romance", "comedy", "drama", "fantasy"], 2),
                      "rating": rating,
                      "episodes": random.randint(1, 16) if category == "drama" else None,
                      "duration": random.randint(22, 60) if category == "drama" else random.randint(90, 180),
                      "streaming_url": f"https://example.com/stream/{category}/{i}",
                      "last_updated": datetime.now().isoformat()
                  })
              
              return content
          
          # Fonction pour exporter les données en JSON
          def export_to_json(filename, data):
              with open(filename, "w", encoding="utf-8") as f:
                  json.dump(data, f, ensure_ascii=False, indent=2)
              print(f" Fichier exporté: {filename}")
          
          # Création du répertoire d'export
          export_path = "export_data"
          if not os.path.exists(export_path):
              os.makedirs(export_path)
          
          # Génération des données principales
          print(" Génération des données de contenu...")
          content = generate_demo_content(30)
          export_to_json(f"{export_path}/content.json", content)
          
          # Génération des catégories
          print(" Génération des catégories...")
          categories = [
              {"id": "drama", "name": "Dramas", "count": 15},
              {"id": "movie", "name": "Films", "count": 8},
              {"id": "anime", "name": "Animés", "count": 7}
          ]
          export_to_json(f"{export_path}/categories.json", categories)
          
          # Génération des contenus mis en avant
          print(" Génération des contenus mis en avant...")
          featured = generate_demo_content(5)
          for item in featured:
              item["featured"] = True
              item["highlight"] = random.choice([True, False])
          export_to_json(f"{export_path}/featured.json", featured)
          
          # Génération des contenus populaires
          print(" Génération des contenus populaires...")
          popular = generate_demo_content(10)
          for item in popular:
              item["views"] = random.randint(1000, 50000)
          export_to_json(f"{export_path}/popular.json", popular)
          
          # Génération des contenus récents
          print(" Génération des contenus récents...")
          recent = generate_demo_content(10)
          export_to_json(f"{export_path}/recently.json", recent)
          
          # Génération des contenus les mieux notés
          print(" Génération des contenus les mieux notés...")
          top_rated = generate_demo_content(10)
          for item in top_rated:
              item["rating"] = round(random.uniform(8.0, 9.9), 1)
          export_to_json(f"{export_path}/topRated.json", top_rated)
          
          print(" Génération des données terminée avec succès!")
          EOF
          
          # Exécution du script Python
          python temp_scraping.py
          
          # Vérification des fichiers générés
          ls -la export_data/
          
          echo "✅ Génération des données terminée avec succès!"
      
      - name: Vérification du contenu S3
        id: check-s3-content
        run: |
          echo " Vérification du contenu dans le bucket S3..."
          
          # Vérifier si le bucket contient des données
          CONTENT_COUNT=$(aws s3 ls s3://flodrama-content-1745269660 --recursive | grep -c "content/")
          
          if [ $CONTENT_COUNT -gt 0 ]; then
            echo " Contenu trouvé dans le bucket S3: $CONTENT_COUNT fichiers"
            echo "HAS_CONTENT=true" >> $GITHUB_OUTPUT
          else
            echo " Aucun contenu trouvé dans le bucket S3"
            echo "HAS_CONTENT=false" >> $GITHUB_OUTPUT
          fi
      
      - name: Upload vers S3
        if: steps.check-s3-content.outputs.HAS_CONTENT == 'true'
        run: |
          echo "📤 Upload des données vers S3..."
          
          # Upload des données vers S3
          aws s3 sync export_data/ s3://${{ env.S3_BUCKET }}/data/ --cache-control "max-age=3600"
          echo "✅ Upload vers S3 terminé avec succès!"
      
      # Étape d'invalidation du cache CloudFront avec gestion d'erreur
      - name: Invalidation du cache CloudFront
        if: steps.check-s3-content.outputs.HAS_CONTENT == 'true'
        run: |
          echo "🔄 Tentative d'invalidation du cache CloudFront..."
          
          # Vérifier si le secret est disponible
          if [ -n "${{ secrets.CLOUDFRONT_DISTRIBUTION_ID }}" ]; then
            # Tenter l'invalidation avec gestion d'erreur
            if aws cloudfront create-invalidation --distribution-id "${{ secrets.CLOUDFRONT_DISTRIBUTION_ID }}" --paths "/data/*"; then
              echo "✅ Cache CloudFront invalidé avec succès"
            else
              echo "⚠️ Échec de l'invalidation du cache CloudFront"
              echo "⚠️ Vérifiez que l'ID de distribution est correct"
              # Ne pas faire échouer le workflow pour cette erreur
              exit 0
            fi
          else
            echo "⚠️ Aucun ID de distribution CloudFront n'est défini dans les secrets"
            echo "⚠️ L'invalidation du cache CloudFront est ignorée"
          fi
      
      - name: Génération des fichiers de contenu agrégé
        if: steps.check-s3-content.outputs.HAS_CONTENT == 'true'
        run: |
          echo "🔄 Génération des fichiers de contenu agrégé pour le frontend..."
          
          # Installer les dépendances Python nécessaires
          pip install boto3
          
          # Rendre le script exécutable
          chmod +x ./scripts/generate_aggregated_content.py
          
          # Exécuter le script de génération de contenu
          python ./scripts/generate_aggregated_content.py
          
          # Vérifier que les fichiers ont été générés
          echo "✅ Vérification des fichiers générés dans S3..."
          aws s3 ls s3://flodrama-content-1745269660 --recursive | grep -E 'featured|popular|recently|topRated|categories|metadata'
      
      # Vérification de l'existence de la fonction Lambda
      - name: Vérification de la fonction Lambda
        id: check-lambda
        run: |
          echo "🔍 Vérification de l'existence de la fonction Lambda..."
          
          # Vérifier si la fonction Lambda existe déjà
          LAMBDA_EXISTS=$(aws lambda list-functions --query "Functions[?FunctionName=='FloDramaImageOptimizer'].FunctionName" --output text || echo "")
          
          if [ -n "$LAMBDA_EXISTS" ]; then
            echo "✅ La fonction Lambda FloDramaImageOptimizer existe"
            echo "LAMBDA_EXISTS=true" >> $GITHUB_OUTPUT
          else
            echo "⚠️ La fonction Lambda FloDramaImageOptimizer n'existe pas"
            echo "LAMBDA_EXISTS=false" >> $GITHUB_OUTPUT
          fi
      
      # Création de la fonction Lambda si elle n'existe pas
      - name: Création de la fonction Lambda
        if: steps.check-lambda.outputs.LAMBDA_EXISTS == 'false'
        run: |
          echo "🚀 Création de la fonction Lambda FloDramaImageOptimizer..."
          
          # Vérifier si le dossier Lambda/image-optimizer existe
          if [ ! -d Lambda/image-optimizer ]; then
            echo "⚠️ Dossier Lambda/image-optimizer non trouvé, création d'une fonction Lambda minimale"
            mkdir -p Lambda/image-optimizer
            echo 'exports.handler = async (event) => {
              console.log("Event:", JSON.stringify(event, null, 2));
              
              return {
                statusCode: 200,
                headers: {
                  "Content-Type": "application/json",
                  "Access-Control-Allow-Origin": "*"
                },
                body: JSON.stringify({
                  message: "FloDrama Image Optimizer fonctionne correctement!",
                  timestamp: new Date().toISOString(),
                  event: event
                })
              };
            };' > Lambda/image-optimizer/index.js
            
            # Création d'un package.json minimal
            echo '{
              "name": "flodrama-image-optimizer",
              "version": "1.0.0",
              "description": "Fonction Lambda@Edge pour l'\''optimisation des images dans FloDrama",
              "main": "index.js",
              "dependencies": {
                "aws-sdk": "^2.1500.0",
                "sharp": "^0.33.0"
              }
            }' > Lambda/image-optimizer/package.json
          fi
          
          # Création d'un répertoire temporaire pour le déploiement
          mkdir -p lambda_package
          
          # Préparation du code Lambda
          cp -r Lambda/image-optimizer/* lambda_package/
          
          # Installation des dépendances dans le package
          cd lambda_package
          npm install --production
          zip -r ../lambda_package.zip .
          cd ..
          
          # Vérification que le rôle IAM existe et a les permissions nécessaires
          echo "🔍 Vérification du rôle IAM..."
          ROLE_ARN="${{ secrets.AWS_LAMBDA_ROLE_ARN }}"
          
          if [ -z "$ROLE_ARN" ]; then
            echo "❌ La variable AWS_LAMBDA_ROLE_ARN n'est pas définie dans les secrets GitHub"
            echo "Création d'un rôle IAM temporaire pour Lambda..."
            
            # Créer une politique de confiance pour Lambda
            python3 -c "import json; print(json.dumps({
              'Version': '2012-10-17',
              'Statement': [{
                'Effect': 'Allow',
                'Principal': {
                  'Service': 'lambda.amazonaws.com'
                },
                'Action': 'sts:AssumeRole'
              }]
            }))" > trust-policy.json
            
            # Créer le rôle IAM
            ROLE_RESPONSE=$(aws iam create-role --role-name FloDramaLambdaRole --assume-role-policy-document file://trust-policy.json)
            ROLE_ARN=$(echo $ROLE_RESPONSE | jq -r '.Role.Arn')
            
            # Attacher la politique AWSLambdaBasicExecutionRole
            aws iam attach-role-policy --role-name FloDramaLambdaRole --policy-arn arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
            
            # Attendre que le rôle soit propagé
            echo "⏳ Attente de la propagation du rôle IAM (10 secondes)..."
            sleep 10
          fi
          
          # Création de la fonction Lambda avec gestion d'erreur
          echo "🚀 Création de la fonction Lambda avec le rôle: $ROLE_ARN"
          aws lambda create-function \
            --function-name FloDramaImageOptimizer \
            --runtime nodejs18.x \
            --role "$ROLE_ARN" \
            --handler index.handler \
            --timeout 30 \
            --memory-size 512 \
            --zip-file fileb://lambda_package.zip \
            --environment "Variables={\"NODE_ENV\":\"production\",\"S3_BUCKET\":\"${{ env.S3_BUCKET }}\",\"FRONTEND_URL\":\"https://flori92.github.io/FloDrama\"}" || {
              echo "❌ Erreur lors de la création de la fonction Lambda"
              echo "⚠️ Vérifiez que le rôle IAM a les permissions nécessaires et que l'utilisateur AWS a les droits pour créer des fonctions Lambda"
              echo "⚠️ Continuer le workflow sans la fonction Lambda..."
              exit 0
            }
          
          echo "✅ Fonction Lambda créée avec succès"
      
      # Mise à jour de la fonction Lambda si elle existe
      - name: Mise à jour de la fonction Lambda
        if: steps.check-lambda.outputs.LAMBDA_EXISTS == 'true'
        run: |
          echo "🔄 Mise à jour de la fonction Lambda FloDramaImageOptimizer..."
          
          # Préparation du code Lambda
          mkdir -p lambda_package
          cp -r Lambda/image-optimizer/* lambda_package/
          
          # Installation des dépendances dans le package
          cd lambda_package
          npm install --production
          zip -r ../lambda_package.zip .
          cd ..
          
          # Mise à jour du code de la fonction Lambda
          aws lambda update-function-code \
            --function-name FloDramaImageOptimizer \
            --zip-file fileb://lambda_package.zip
            
          # Mise à jour de la configuration de la fonction Lambda
          aws lambda update-function-configuration \
            --function-name FloDramaImageOptimizer \
            --timeout 30 \
            --memory-size 512 \
            --environment "Variables={\"NODE_ENV\":\"production\",\"S3_BUCKET\":\"${{ env.S3_BUCKET }}\",\"FRONTEND_URL\":\"https://flori92.github.io/FloDrama\"}"
          
          echo "✅ Fonction Lambda mise à jour avec succès"
      
      # Nettoyage des anciennes versions Lambda
      - name: Nettoyage des anciennes versions Lambda
        if: steps.check-lambda.outputs.LAMBDA_EXISTS == 'true'
        run: |
          echo "🧹 Nettoyage des anciennes versions Lambda..."
          
          # Liste des versions sauf $LATEST et les 3 plus récentes
          VERSIONS_TO_DELETE=$(aws lambda list-versions-by-function --function-name FloDramaImageOptimizer --query "Versions[?Version!='$LATEST'].Version" --output text | tr '\t' '\n' | sort -n | head -n -3)
          
          # Suppression des anciennes versions
          for VERSION in $VERSIONS_TO_DELETE; do
            echo "Suppression de la version $VERSION..."
            aws lambda delete-function --function-name FloDramaImageOptimizer:$VERSION || true
          done
          
          # Configuration de la rétention des logs CloudWatch
          echo "⏱️ Configuration de la rétention des logs CloudWatch..."
          aws logs put-retention-policy --log-group-name /aws/lambda/FloDramaImageOptimizer --retention-in-days 30
          
          echo "✅ Nettoyage des anciennes versions Lambda terminé"
      
  frontend:
    runs-on: ubuntu-latest
    needs: backend
    steps:
      - name: Checkout du code
        uses: actions/checkout@v4
      
      - name: Configuration de Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: Frontend/package-lock.json
      
      - name: Construction du frontend
        run: |
          echo "🔨 Construction du frontend FloDrama..."
          
          # Vérifier si le dossier Frontend existe
          if [ -d "Frontend" ]; then
            echo "📁 Dossier Frontend trouvé, préparation de la construction..."
            
            cd Frontend
            
            # Installation des dépendances nécessaires
            echo "📦 Installation des dépendances React..."
            npm install --legacy-peer-deps
            
            # Vérifier si vite.config.ts existe, sinon le créer
            if [ ! -f vite.config.ts ] && [ ! -f vite.config.js ]; then
              echo "⚙️ Configuration Vite non trouvée, création d'une configuration adaptée..."
              cat > vite.config.ts << 'VITECONFIG'
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";
import path from "path";

export default defineConfig({
  plugins: [react()],
  base: "/FloDrama/",
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
  build: {
    outDir: "../dist",
    emptyOutDir: true,
    sourcemap: true,
    minify: true,
    cssCodeSplit: true,
  },
  css: {
    preprocessorOptions: {
      scss: {
        additionalData: `
          $primary-color: #3b82f6;
          $secondary-color: #d946ef;
          $background-color: #121118;
        `
      }
    }
  }
});
VITECONFIG
            fi
            
            # Créer un .env avec les variables d'environnement nécessaires
            echo "🔑 Configuration des variables d'environnement..."
            cat > .env << ENVFILE
VITE_API_URL=https://api.flodrama.com
VITE_S3_BUCKET=${{ env.S3_BUCKET }}
VITE_AWS_REGION=${{ env.AWS_REGION }}
VITE_SITE_URL=https://flori92.github.io/FloDrama
ENVFILE
            
            # Construction du frontend
            echo "🚀 Construction de l'application React..."
            npm run build
            
            # Vérifier si la construction a réussi
            if [ $? -eq 0 ]; then
              echo "✅ Construction du frontend réussie!"
            else
              echo "❌ Échec de la construction du frontend, création d'une page d'attente temporaire..."
              
              # Créer une page d'attente temporaire avec le design FloDrama
              mkdir -p ../dist
              cat > ../dist/index.html << 'HTMLFILE'
<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>FloDrama - Votre plateforme de streaming</title>
  <style>
    :root {
      --color-primary: #3b82f6;
      --color-secondary: #d946ef;
      --color-background: #121118;
      --color-text: #ffffff;
    }
    
    body {
      font-family: "SF Pro Display", -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
      background-color: var(--color-background);
      color: var(--color-text);
      margin: 0;
      padding: 0;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      text-align: center;
    }
    
    .container {
      max-width: 800px;
      padding: 2rem;
    }
    
    .logo {
      font-size: 4rem;
      font-weight: bold;
      margin-bottom: 2rem;
      background: linear-gradient(to right, var(--color-primary), var(--color-secondary));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      animation: scale 2s infinite alternate;
    }
    
    @keyframes scale {
      from { transform: scale(1); }
      to { transform: scale(1.05); }
    }
    
    h1 {
      font-size: 2.5rem;
      margin-bottom: 1rem;
    }
    
    p {
      font-size: 1.2rem;
      margin-bottom: 2rem;
      opacity: 0.9;
    }
    
    .btn {
      display: inline-block;
      background-color: var(--color-text);
      color: var(--color-background);
      padding: 0.75rem 1.5rem;
      border-radius: 0.5rem;
      text-decoration: none;
      font-weight: bold;
      transition: all 0.3s ease;
      border: none;
      cursor: pointer;
    }
    
    .btn:hover {
      transform: translateY(-3px);
      box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
    }
    
    .btn-outline {
      background-color: transparent;
      border: 1px solid var(--color-text);
      color: var(--color-text);
      margin-left: 1rem;
    }
    
    .btn-outline:hover {
      background-color: rgba(255, 255, 255, 0.1);
    }
    
    .loading {
      display: flex;
      margin: 2rem 0;
    }
    
    .loading div {
      width: 12px;
      height: 12px;
      margin: 0 5px;
      border-radius: 50%;
      background: linear-gradient(to right, var(--color-primary), var(--color-secondary));
      animation: bounce 1.5s infinite alternate;
    }
    
    .loading div:nth-child(2) {
      animation-delay: 0.2s;
    }
    
    .loading div:nth-child(3) {
      animation-delay: 0.4s;
    }
    
    @keyframes bounce {
      from { transform: translateY(0); }
      to { transform: translateY(-20px); }
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="logo">FloDrama</div>
    <h1>Votre plateforme de streaming asiatique</h1>
    <p>Découvrez les meilleurs dramas, films et animés asiatiques en un seul endroit.</p>
    
    <div class="loading">
      <div></div>
      <div></div>
      <div></div>
    </div>
    
    <p>Notre site est en cours de déploiement et sera bientôt disponible.</p>
    
    <div>
      <button class="btn">Être notifié au lancement</button>
      <button class="btn btn-outline">En savoir plus</button>
    </div>
  </div>
</body>
</html>
HTMLFILE
            fi
            
            cd ..
          else
            echo "❌ Dossier Frontend non trouvé, création d'une page d'attente..."
            
            # Créer le dossier dist s'il n'existe pas
            mkdir -p dist
            
            # Créer une page d'attente avec le design FloDrama
            cat > dist/index.html << 'HTMLFILE'
<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>FloDrama - Votre plateforme de streaming</title>
  <style>
    :root {
      --color-primary: #3b82f6;
      --color-secondary: #d946ef;
      --color-background: #121118;
      --color-text: #ffffff;
    }
    
    body {
      font-family: "SF Pro Display", -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
      background-color: var(--color-background);
      color: var(--color-text);
      margin: 0;
      padding: 0;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      text-align: center;
    }
    
    .container {
      max-width: 800px;
      padding: 2rem;
    }
    
    .logo {
      font-size: 4rem;
      font-weight: bold;
      margin-bottom: 2rem;
      background: linear-gradient(to right, var(--color-primary), var(--color-secondary));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      animation: scale 2s infinite alternate;
    }
    
    @keyframes scale {
      from { transform: scale(1); }
      to { transform: scale(1.05); }
    }
    
    h1 {
      font-size: 2.5rem;
      margin-bottom: 1rem;
    }
    
    p {
      font-size: 1.2rem;
      margin-bottom: 2rem;
      opacity: 0.9;
    }
    
    .btn {
      display: inline-block;
      background-color: var(--color-text);
      color: var(--color-background);
      padding: 0.75rem 1.5rem;
      border-radius: 0.5rem;
      text-decoration: none;
      font-weight: bold;
      transition: all 0.3s ease;
      border: none;
      cursor: pointer;
    }
    
    .btn:hover {
      transform: translateY(-3px);
      box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
    }
    
    .btn-outline {
      background-color: transparent;
      border: 1px solid var(--color-text);
      color: var(--color-text);
      margin-left: 1rem;
    }
    
    .btn-outline:hover {
      background-color: rgba(255, 255, 255, 0.1);
    }
    
    .loading {
      display: flex;
      margin: 2rem 0;
    }
    
    .loading div {
      width: 12px;
      height: 12px;
      margin: 0 5px;
      border-radius: 50%;
      background: linear-gradient(to right, var(--color-primary), var(--color-secondary));
      animation: bounce 1.5s infinite alternate;
    }
    
    .loading div:nth-child(2) {
      animation-delay: 0.2s;
    }
    
    .loading div:nth-child(3) {
      animation-delay: 0.4s;
    }
    
    @keyframes bounce {
      from { transform: translateY(0); }
      to { transform: translateY(-20px); }
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="logo">FloDrama</div>
    <h1>Votre plateforme de streaming asiatique</h1>
    <p>Découvrez les meilleurs dramas, films et animés asiatiques en un seul endroit.</p>
    
    <div class="loading">
      <div></div>
      <div></div>
      <div></div>
    </div>
    
    <p>Notre site est en cours de déploiement et sera bientôt disponible.</p>
    
    <div>
      <button class="btn">Être notifié au lancement</button>
      <button class="btn btn-outline">En savoir plus</button>
    </div>
  </div>
</body>
</html>
HTMLFILE
          fi
          
          # Vérification du contenu du dossier dist
          echo "📂 Vérification du contenu du dossier dist..."
          ls -la dist
      
      - name: Déploiement sur GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist
          branch: gh-pages
          clean: true
          
      - name: Notification frontend
        run: echo " Frontend déployé avec succès sur GitHub Pages"
